---
- name: Restart snmpd service
  service:
    name: 'snmpd'
    enabled: '{{ snmpd_enabled | default(false) }}'
    state: '{{ "restarted" if snmpd_enabled | default(false) else "stopped" }}'
  become: true
